apiVersion: v1
kind: Service
metadata:
  name: headless-service
spec:
  selector:
    dns: test
  clusterIP: None

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: Test-DNS
  labels:
    dns: test
spec:
  replicas: 3
  selector:
    matchLabels:
      dns: test
  template:
    metadata:
      labels:
        dns: test
    spec:
      hostname: hello-dns
      subdomain: headless-service
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        -  containerPort: 80